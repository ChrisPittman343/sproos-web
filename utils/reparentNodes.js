/**
 * Constructs a sorted JSON tree of nodes from an
 * array of unsorted nodes.
 * @param {Array} nodes
 * @returns JSON tree of reparented nodes
 */
export function reparentNodes(nodes) {
  const tree = {};
  nodes.forEach((node) => {});
}
